
// Sidebar toggle is handled globally in js/script.js (loaded on every page).
// This file is intentionally a no-op fallback so it does NOT re-bind the
// menu button (which would double-toggle). The canonical logic in
// script.js uses the `sidebar-hidden` class on #sidebar and persists
// state to localStorage, working on all screen sizes.

